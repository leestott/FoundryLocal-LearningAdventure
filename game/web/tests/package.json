{
  "name": "foundry-game-screenshots",
  "version": "1.0.0",
  "description": "Playwright tests for capturing game screenshots",
  "scripts": {
    "test": "npx playwright test",
    "screenshot": "npx playwright test screenshot.spec.js",
    "install-browsers": "npx playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
